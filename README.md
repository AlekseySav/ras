# ras

## build

- build:   `$ build/config ras release && make`
- clear:   `$ build/clear`
- tests:   `$ build/run-tests`
- install: `# install ras /bin`

## required libraries
- gtest
- gtest_main
- fmt
